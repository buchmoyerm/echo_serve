#!/usr/bin/env python

import os

setup(name='echo_server',
      version='1.0',
      description='simple echo server',
      author='Mark Buchmoyer',
      url='github.com/buchmoyerm/echo_serve',
      intall_requires=['flask'],
      packages=find_packages()
      )
